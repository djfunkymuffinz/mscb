<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <title>MSCB Site</title>
   <base href="/">
   <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
   <link rel="stylesheet" href="https://code.getmdl.io/1.2.1/material.indigo-pink.min.css">
   <script defer src="https://code.getmdl.io/1.2.1/material.min.js"></script>
   <link rel="stylesheet" href="assets/styles.css"></link>
</head>

<div class="mdl-layout mdl-js-layout mdl-layout--fixed-header mdl-layout--fixed-tabs">
   <header class="mscb-header mdl-layout__header mdl-layout__header--scroll">
      <div class="mdl-layout__header-row">
         <span class="mdl-layout-title">Mid Shore<br>Community Band</span>
         <div class="mdl-layout-spacer"></div>
         <div class="header-info">Serving the<br>
            Middle Eastern Shore of Maryland<br><br>
            EJ Oesterle - Music Director<br>
            Matt Fullerton - Music Director Emeritus<br>
         </div>
      </div>
      <div class="mdl-layout__tab-bar mdl-js-ripple-effect">
         <% for (const tab of tabs) { %>
            <% if (renderTab(tab)) { %>
               <a href="#<%= tab.id %>" id="<%=tab.id%>" class="mdl-layout__tab<%= tab.isDefault ? ' is-active' : '' %>"><%= tab.title %></a>
               <div class="mdl-tooltip mdl-tooltip--large" for="<%=tab.id%>">
                  <%=tab.tooltip%>
               </div>
            <% }%>
         <% } %>
      </div>
   </header>
   <main class="mdl-layout__content">
      <% for (const tab of tabs) { %>
         <section class="mdl-layout__tab-panel<%= tab.isDefault ? ' is-active' : '' %>" id="<%= tab.id %>">
            <%- include(tab.template) %>
         </section>
      <% } %>
   </main>
   <footer class="mdl-mini-footer">

      <div class="mdl-mini-footer__left-section">
         <div class="mdl-logo">
            More Information
         </div>
         <ul class="mdl-mini-footer__link-list">
            <li><a href="#">About Us</a></li>
            <li><a href="mailto:info@mscb.org">Contact MSCB</a></li>
         </ul>
      </div>

      <div class="mdl-mini-footer__right-section">
         <form action="http://www.facebook.com/pages/Mid-Shore-Community-Band-Maryland/128007733896366">
            <button type="submit" class="social-btn social-btn--facebook"></button>
         </form>
      </div>
   </footer>
</div>
</html>
